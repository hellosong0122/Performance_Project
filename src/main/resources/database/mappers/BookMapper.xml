<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
   <mapper namespace="com.per.main.per.BookDAO">
  
  <select id="getPerList" resultType="PerformanceDTO" parameterType="Pager">
		SELECT PRFNM, PRFPDFROM, PRFPDTO, GENRENM
		FROM (SELECT ROWNUM R, F.*
			  FROM (SELECT *
			  		FROM PERFORMANCE) F)
		<!-- WHERE R BETWEEN #{startRow} AND #{lastRow} --> 
	</select>
	
	<select id="getPlaceList" resultType="PerformancePlaceDTO" parameterType="Pager">
		SELECT FCLTYNM, ADRES, TELNO
		FROM (SELECT ROWNUM R, P.*
		 	  FROM (SELECT * 
			  	    FROM PERFORMANCE_PLACE) P)
		<!-- WHERE R BETWEEN #{startRow} AND #{lastRow}  -->
	</select> 
	

<select id="getPlaceDetails" resultType="PerformancePlaceDTO" parameterType="String">
    SELECT FCLTYNM, ADRES, TELNO
    FROM PERFORMANCE_PLACE
    WHERE MT10ID = #{mt10id}
</select>

  <select id="getPerTime" resultType="PerformanceDTO" parameterType="Pager">
		SELECT PRFNM, PRFPDFROM, PRFPDTO, GENRENM, dtguidance, performance_num
		FROM (SELECT ROWNUM R, F.*
			  FROM (SELECT *
			  		FROM PERFORMANCE) F)
		<!-- WHERE R BETWEEN #{startRow} AND #{lastRow} --> 
	</select>
	
	<select id="getSeat" resultType="SeatDTO">
		SELECT
			*
		FROM
			SEAT
	</select>


	    <!-- BOOK 테이블에서 모든 예약 정보 -->
    <select id="getBookList" resultType="BookDTO">
        SELECT * FROM BOOK
    </select>
	
</mapper>